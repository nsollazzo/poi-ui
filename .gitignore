node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist
/coverage
*.tgz

# svelte-package may emit .d.ts next to non-lib .svelte sources during build.
# Scoped to src so the published dist/**/*.svelte.d.ts types are unaffected.
src/**/*.svelte.d.ts

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*storybook.log
storybook-static

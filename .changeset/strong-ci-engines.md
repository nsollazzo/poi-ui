---
'@nsollazzo/poi-ui': patch
---

Declare supported `engines` in `package.json` (Node >= 22.12, pnpm >= 10). This is the only consumer-visible change in a larger internal CI/CD and supply-chain hardening pass.
